# This file marks the directory as a Python *package*.
# A package is just a folder that Python treats as a collection of modules.
# Without this file, you couldn't do "from spotlight_windows.config import Config".

__version__ = "0.1.0"
